# zsh is the macOS login shell; interactive work happens in fish (Ghostty).
# Nix and Homebrew paths come from /etc/zshrc (nix-darwin, incl. `brew shellenv`).

# Added by OrbStack: command-line tools and integration
source ~/.orbstack/shell/init.zsh 2>/dev/null || :
